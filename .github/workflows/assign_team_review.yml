name: "Review"
on:
  pull_request:

jobs:
  assign-reviewers:
    runs-on: ubuntu-latest
    steps:
      - name: "Assign team"
        uses: rowi1de/auto-assign-review-teams@3c2f3255c753fbb2ec6851d3d7c04921dd7135c3
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          teams: "flutter-package-maintainers"
