name: example
description: A new Flutter project.

version: 0.0.1

environment:
  sdk: ">=2.10.0-0.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  adaptive_breakpoints:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
